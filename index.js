document.getElementById("view_today").innerHTML = getToday(number());

function getToday(n) {
	for (var i = 0; i < n; i++) {
        var newDiv = document.createElement("button");
    }
	return s;
}

function number() {
    // 課題の数を取得
    return 3;
}